Learning OS dev with AI ... blog - generate a static website from it


# Install

1. Install the RISC-V toolchain
```bash
rustup target add riscv64gc-unknown-none-elf
```

2. Install `qemu-system-riscv64` `gdb-multiarch`
